// Parsing tests live in crates/blocklist/src/lib.rs under #[cfg(test)] mod tests.
// Network-dependent integration tests are intentionally omitted to keep the
// test suite reproducible and CI-friendly.
